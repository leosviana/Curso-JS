 var inicio = 1;
    var fim = 3;
    var passo = 1;
    for(var passo = inicio; inicio <= fim; passo++){
        // res.innerHTML = `${inicio}`;
        console.log(`${inicio} e ${fim} e ${passo}`);
    }